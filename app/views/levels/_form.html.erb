<section class="section">
	<div class="container">
		<%= simple_form_for(@level) do |f| %>
		 <%= f.error_notification %>
		 <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>		  
		 <div class="field">
		  <div class="control">
			<%= f.input :title, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'string' }, label:"Niveau:", placeholder:"Ajouter un Niveau..", autofocus: true %>
		  </div>
		 </div>
		  <%= f.button :submit, "Ajouter", class: 'button is-info' %>
		<% end %>
	</div>
</section>