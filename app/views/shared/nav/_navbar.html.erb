<!--HEADER/!-->

<!-- no header render on the home page/!-->
<% if user_signed_in? %>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container">
      <%= link_to root_path, class:"navbar-brand logo " do %>
        CHATLN        
      <% end  %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item active">
          <!--administration</!-->
          <% if admin_role(current_user) %>
            <li class="nav-item active">
                      <%= link_to manager_index_path, class:"m-links nav-link add" do %>
                       <i class="fa fa-plus" aria-hidden="true"></i> Admin
                      <% end %>
                    </li>
            <% end %>
          </li>
          <li class="nav-item active">
            <%= link_to root_path, class:"m-links nav-link acceuil" do %>              
             <i class="fa fa-home"></i> Acceuil
            <% end %>
          </li>
          <!-- user avatar/!-->
          <li class="nav-item active">
            <%= link_to user_path(current_user), class:"m-links nav-link avatar" do %>
               <i class="fa fa-user-circle-o" aria-hidden="true"></i>
               <%= current_user.username %>
            <% end %>           

          </li>
          <% if status_user(current_user) %>            
          <li class="nav-item active">
            <%= link_to new_course_path, class:"m-links nav-link add" do %>
             <i class="fa fa-plus-circle" aria-hidden="true"></i> Ajouter Cours
            <% end %>
          </li>
          <% end %>
          <li class="nav-item active">
            <%= link_to destroy_user_session_path, method: :delete, class:"m-links nav-link sign_out" do %>
             <i class="fa fa-power-off" aria-hidden="true"></i> Déconnexion
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<% end %>

